import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { EcopointPage } from './ecopoint';

@NgModule({
  declarations: [
    EcopointPage,
  ],
  imports: [
    IonicPageModule.forChild(EcopointPage),
  ],
})
export class EcopointPageModule {}
